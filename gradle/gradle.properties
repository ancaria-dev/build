org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configuration-cache=true
org.gradle.configuration-cache.problems=fail

group=dev.ancaria.coderpack
version=0.101.1

# The artifact version of dev.ancaria.coderpack:api, published by coderpack. Not
# the api = "[3,4)" line in a descriptor, which follows Verifier.API and changes
# only when the contract breaks. The plugin offers this as sacred.apiVersion and the
# scaffolder writes it into every project it generates.
apiVersion=0.200.0
