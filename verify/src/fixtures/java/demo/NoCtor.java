package demo;

import dev.ancaria.coderpack.api.SacredMod;

/** Wants an argument the loader has no way to pass. */
public final class NoCtor extends SacredMod {

    public NoCtor(String name) {
    }
}
