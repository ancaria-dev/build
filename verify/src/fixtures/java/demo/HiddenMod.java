package demo;

import dev.ancaria.coderpack.api.SacredMod;

/**
 * Package-private, with a public constructor so that visibility is the only
 * thing wrong with it.
 */
class HiddenMod extends SacredMod {

    public HiddenMod() {
    }
}
