package demo;

import dev.ancaria.coderpack.api.SacredMod;

/** Public, with a no-argument constructor nothing outside the class may call. */
public final class HiddenCtor extends SacredMod {

    private HiddenCtor() {
    }
}
